name: "Deploy first Workflow"

on:
  push:
    branches: [main]

jobs:
  first-job:
  runs-on: ubuntu-latest
  steps:
    - run: echo "Hacemos pull del codigo"
    - run: echo "correr migraciones"
    - run: echo "Generear el build"
    - run: echo "publicar el build"