name: "Deploy first Workflow"

on:
  push:
    branches: ["main"]

jobs:
  first-job:
    runs-on: ubuntu-latest
    env:
      NAME: "world"

    steps:
      - name: working dir
        run: ls -la
      
      - name: Action
        uses: actions/checkout@v4

      - name: working dir after action
        run: ls -la
      
  second-job:
    if: ${{ github.ref == 'refs/heads/main' }}
    needs: first-job
    runs-on: ubuntu-latest
    steps:
      - name: "hola mundo 2"
        run: echo "correr migraciones ${{ github.ref }}"